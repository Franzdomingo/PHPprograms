$(document).ready(function() {
    var randomNumber = parseInt($("#random-number").text());
    var randomString = $("#random-string").text();

    alert("Random Number: " + randomNumber + "\nRandom String: " + randomString);
});
